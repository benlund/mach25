Fri 2nd Dec 2011

 * Preparing for Space Wiki Hack Day - install Mongo
  - http://fastdl.mongodb.org/osx/mongodb-osx-x86_64-2.0.1.tgz
  - copying into /usr/local and symlinking /usr/local/mongodb to it
  - in ~/Development/SpaceWiki:
   $ mkdir -p data/db
   $ mkdir -p logs
   $ /usr/local/mongodb/bin/mongod --dbpath data/db --fork --logpath logs/mongod.log --logappend
   $ /usr/local/mongodb/bin/mongo (for the shell)
  - lookup whether we can use devise with mongo -- yes!


----

Sat 3rd Dec

 * Getting started
  - create github project
  - created 1.9.2@mach25 gemset
  - install rails 3.1.3 in gemset
  - creating rails project
   $ rails new .
   $ gem update bundler (for Gem::SlientUI error)
   $ bundle install
  - setting up devise with mongo
   - actually, just use the template from here: https://github.com/RailsApps/rails3-mongoid-devise/wiki/Tutorial
   - so remove the rails-gen stuff and retry
   $ rails new . -m https://github.com/RailsApps/rails3-application-templates/raw/master/rails3-mongoid-devise-template.rb -T -O
    - nope, it failed trying to edit user_controller (which doens't exist)
    - but it seems to be ok anyway
    - start mongod
    $ mkdir -p data/db
    $ /usr/local/mongodb/bin/mongod --dbpath data/db --fork --logpath data/mongod.log --logappend
    - ok, seems to start

 * Added placeholder login / signup links

 * Creating rocket engines data type
  - set key :name
  - try it in console
  - created controller and views

 * Added versioning
  - mongoid has it build in! wow
  - ok, so adding a version history action


@@ designations
@@ linking to other entities in descriptions
